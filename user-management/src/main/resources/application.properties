server.port=8080

# Spring Data JPA and Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
# `update` ?????????????????
# ???????? `none` ? `validate`???? Flyway/Liquibase ???????
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/user_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=qcgao2
spring.datasource.password=Ph97in10!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Thymeleaf Configuration (Optional, default is usually fine)
spring.thymeleaf.cache=false

# Application Name
spring.application.name=UserManagementApp